{
  "name": "devpulse",
  "version": "1.0.0",
  "description": "DevPulse — API health monitoring SaaS (monorepo: frontend + backend)",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "start": "node backend/src/server.js",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "db:migrate:deploy": "npm --prefix backend run db:migrate:deploy",
    "db:studio": "npm --prefix backend run db:studio",
    "db:seed": "npm --prefix backend run db:seed"
  },
  "license": "MIT"
}
